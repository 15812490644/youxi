# Spec: 《冒险家与伙伴》—— 变种贪吃蛇 3D 版

Status: ready-for-agent
Feature: adventurer-party
Created: 2026-09-09
Seam: 无头逻辑层（复用 ADR-0006 的 eventLog）

> 本 spec 综合 `docs/PROMPT.md`（规则与数值）、`docs/ADR.md`（决策理由）、`docs/GLOSSARY.md`（术语）、`docs/RULES.md`（改动契约）四份已定稿文档。
> **文档未在此重复**——数值、术语、红线一律以四份文档为准，本 spec 只写它们的执行顺序与验证方式。

---

## Problem Statement

验收人不懂编程，也不会用命令行。他想要一个能在自己电脑上**双击就打开**的 3D 游戏：控制一个圆头小人在无尽草原上跑，身后跟着一列动物伙伴，伙伴会替他挡伤害，地上的火山冰川荆棘能被打掉，每五分钟来一个 BOSS。

三个真实痛点：

1. **打不开就全完了。** 之前任何需要起本地服务器、装依赖、跑构建的方案，对他都是不可接受的门槛。
2. **「手感不对」没法改。** 他玩完只会说「太滑了」「乌龟触发了但看不出来」。这类话不落到具体数字上，AI 只能猜，改十轮也改不到点上。
3. **改着改着就认不出来了。** 这个项目会经历几十轮增量修改。最大的风险不是做不出来，是某一轮之后它不再是原来那个游戏——数值被悄悄改掉、命名漂移、模块被合并、功能被静默降级。

顺带一个暗线目标（ADR-0001）：把它当探针，看执行环境在**长会话、大代码量、反复增量修改**下的表现。

## Solution

一个纯前端静态站点，双击 `index.html` 直接运行，运行时零外部请求。

- **真 3D** 用 three.js，库文件本地放 `vendor/`，r128 UMD 版提供全局 `THREE`
- **多文件**，约 15 个模块挂到全局 `window.ADV`，用普通 `<script src>` 按依赖顺序加载（ES modules 在 `file://` 下会被 CORS 拦死）
- **所有可调参数集中在 `config.js`**，验收人调手感只改这一个文件
- **页面内嵌反馈面板**，一键复制运行时状态快照（分数 / `baseHP` / 伙伴队列 / 全量参数 / FPS / 最近 20 条事件日志），把「太滑」翻译成 `MOVE_INTERVAL=150ms`
- **`check.js` 机器校验**，把 9 项硬约束从「AI 的自我承诺」变成「跑不过就报错」
- **git 提交链**作为可复现证据，禁止改写历史

游戏内容按 ADR-0002 **一次做完全量**：6 种伙伴 / 4 种地形 / 3 种普通怪 + 3 种 BOSS / 3 个技能 / 无限地图 / 本地排行榜。

---

## User Stories

### 交付与启动

1. 作为验收人，我希望双击 `index.html` 就能直接开始玩，以便我不需要学任何命令行或安装东西。
2. 作为验收人，我希望游戏运行时不联网，以便我在没网或内网环境也能打开。
3. 作为验收人，我希望打开页面时控制台没有报错，以便我知道它是真能跑而不是碰巧没崩。
4. 作为验收人，我希望画面稳定在 60fps，以便卡顿不会干扰我对「手感」的判断。
5. 作为玩家，我希望看到开始界面上有规则说明入口，以便我不用猜怎么玩。
6. 作为玩家，我希望开始界面能看到历史最高分，以便我知道自己要达到什么水平。

### 移动与队伍

7. 作为玩家，我希望用方向键或 WASD 控制四个方向，以便用键盘就能玩。
8. 作为玩家，我不能让冒险家 180 度直接掉头，以免误触瞬间自杀。
9. 作为玩家，我希望快速连按两次方向时输入不丢，以便急转弯能真的转过去。
10. 作为玩家，我希望伙伴像贪吃蛇一样跟在我身后依次移动，以便我一眼看出队伍有多长。
11. 作为玩家，我希望撞到自己队伍时会掉血并失控 3 秒，以便长队伍是有代价的。
12. 作为玩家，我希望触屏能用滑动操作，以便我在手机上也能玩（此项无真机验证，不影响键盘手感）。

### 伙伴（6 种，全部被动）

13. 作为玩家，我收集的伙伴会加在队伍尾部，以便我刚捡到的会最先被用掉——收集顺序就是策略。
14. 作为玩家，我希望进入火山格时若队尾有小龙，小龙顶上去、那一格火山被烧成平地，以便我用一只伙伴换一格永久安全。
15. 作为玩家，我希望进入冰川格时若有凤凰，冰川被摧毁且减速一起消失，以便我不只是免伤而是真的清掉障碍。
16. 作为玩家，我希望进入荆棘格时若有小狐狸，荆棘被摧毁，以便同上。
17. 作为玩家，我希望撞到铁怪时若有熊猫，铁怪被秒杀并额外给我一格临时血，以便硬闯有回报。
18. 作为玩家，我希望被弓箭手射中时若有乌龟，攻击被挡下且那只弓箭手被反杀，以便远程威胁有解。
19. 作为玩家，我希望击杀怪物且自己未满血时，若有小狼就自动消耗一只回 1 血，以便我能打得更久。
20. 作为玩家，我希望满血时不会白白浪费小狼，以便回复资源不被无意义消耗。
21. 作为玩家，我永远不能主动释放伙伴，以便「伙伴」和「技能」这两类东西不会混淆。
22. 作为玩家，我希望伙伴触发后立刻离队且不可回收，以便每次触发都是真实的取舍。
23. 作为玩家，我希望稀有伙伴（龙/凤凰/熊猫）比普通伙伴更值钱，以便捡到稀有时有惊喜。
24. 作为玩家，我希望底部伙伴栏按队伍顺序显示、同种折叠成「图标×N」，以便队伍很长时也能看清。

### 地形（4 种）

25. 作为玩家，我希望大部分地面是安全草地，以便危险地形是点缀而不是堵路。
26. 作为玩家，我希望踩到火山且没有小龙时扣 1 血并愣住 1 秒，以便失误有代价但不会连锁送命。
27. 作为玩家，我希望踩到冰川且没有凤凰时减速 2 秒并愣住 1 秒，以便它区别于直接扣血。
28. 作为玩家，我希望踩到荆棘且没有小狐狸时扣 1 血并愣住 1 秒，以便三种危险各有手感。
29. 作为玩家，我希望危险地形不会连成片堵死去路，以便我在任何位置都至少有一个方向可走。
30. 作为玩家，我希望出生点周围是安全的，以便我开局不会被瞬间打死。
31. 作为玩家，我希望地图随我移动无限延伸、远处的自动回收，以便我能一直跑下去。
32. 作为玩家，我希望被摧毁的地形在区块重建后可能重新长出来，以便地图不会越走越空。
33. 作为玩家，我希望斩击能把身边的危险地形一起砍平，以便我有主动改造地图的手段。

### 生命值与结算

34. 作为玩家，我希望左上角的心和底部的伙伴数量是两个独立的数，以便我一眼分清「我还有几条命」和「我还有几个挡箭牌」。
35. 作为玩家，我希望受伤时先扣护盾、再消耗对应伙伴、最后才扣基础血，以便优先级可预测。
36. 作为玩家，我希望护盾只是「临时一格血」而不是无敌，以便我不会误以为有盾就能硬闯 BOSS。
37. 作为玩家，我希望被 BOSS 撞（3 点伤害）时护盾只吃掉 1 点、剩下 2 点打在自己身上，以便护盾的强度符合直觉。
38. 作为玩家，我希望重复获得护盾不会叠加成两格，只是把 1 分钟的计时重置，以便囤熊猫无敌的策略不成立。
39. 作为玩家，我希望有对应伙伴时受到的伤害被完全免除，以便收集伙伴真的有用。
40. 作为玩家，我希望踩地形和撞自身导致的失控时长不同（1 秒 vs 3 秒），以便一次小失误不至于送命。
41. 作为玩家，我希望基础血归零时游戏结束并写入排行榜，以便一局有明确终点。
42. 作为玩家，我希望护盾不影响眩晕与硬直的触发，以便它只挡伤害不挡失控。

### 普通怪（3 种）

43. 作为玩家，我希望史莱姆随机乱跳、一撞就死，以便它是低威胁的环境噪声。
44. 作为玩家，我希望弓箭手与我保持距离并周期性放冷箭，以便它逼我主动处理而不是绕开就算了。
45. 作为玩家，我希望铁怪移动极慢且不追我，以便它更像路障而不是猎手。
46. 作为玩家，我希望铁怪撞一下很疼（-2 血），以便绕开它是值得的。
47. 作为玩家，我希望普通移动撞怪时怪物也掉 1 血，以便硬撞是一种可行但昂贵的打法。
48. 作为玩家，我希望撞完怪之后双方都僵直 0.5 秒，以便贴脸连撞不能无脑清怪。
49. 作为玩家，我希望用技能打怪比硬撞划算，以便技能系统有存在意义。

### BOSS（3 种）

50. 作为玩家，我希望每 5 分钟来一只 BOSS，以便长局有节奏变化。
51. 作为玩家，我希望三种 BOSS 轮流出现而不是随机，以便我每局体验不重复。
52. 作为玩家，我希望熔岩兽直线冲锋并在路径上留火痕，以便它威胁的是我的走位路线。
53. 作为玩家，我希望寒霜王带减速光环并不断召唤小怪，以便它压缩我的机动空间。
54. 作为玩家，我希望暗影领主瞬移到我身边再放范围震波，以便我不能一直风筝它。
55. 作为玩家，我希望每种 BOSS 的技能都有躲法（冲刺/护体/走位），以便没有无解伤害。
56. 作为玩家，我希望击杀 BOSS 得 500 分和 3 个伙伴，以便高风险有高回报。
57. 作为玩家，我希望 BOSS 的技能会顺带摧毁地形，以便它对环境有可见影响。

### 技能（3 个，全部主动）

58. 作为玩家，我希望三个技能开局全部可用，以便我第一局就能体验到完整系统再提意见。
59. 作为玩家，我希望冲刺能沿当前方向瞬移 3 格且期间无敌，以便它是逃生手段。
60. 作为玩家，我希望护体给我 2 秒无敌并把周围怪物弹开，以便被围住时有解。
61. 作为玩家，我希望斩击对周围一圈怪物造成 2 点伤害，以便它是清场手段。
62. 作为玩家，我希望三个技能各自独立冷却，以便我不用等一个才能用下一个。
63. 作为玩家，我希望技能图标上有冷却环、冷却中灰显读秒、就绪时高亮，以便我不看键盘也知道能不能放。
64. 作为玩家，我希望技能不消耗伙伴，以便两类资源互不干扰。
65. 作为玩家，我希望技能不会让我自己受伤，以便「技能」和「硬撞」是清晰的对比。
66. 作为玩家，我希望既能按 `1`/`2`/`3` 也能点击图标释放，以便两种习惯都照顾到。

### 计分与排行榜

67. 作为玩家，我希望收集、击杀、打 BOSS 都有分，以便不同玩法都有正反馈。
68. 作为玩家，我希望分数变化时数字有弹跳动画，以便加分被看见。
69. 作为玩家，我希望结束时看到本局分数与历史最高分的对比，以便我知道有没有进步。
70. 作为玩家，我希望结束时看到存活时长和最长队伍，以便我有多维度的成绩。
71. 作为玩家，我希望排行榜保留前 10 名（分数/日期/时长/最长队伍），以便我能追自己的记录。
72. 作为玩家，我希望关掉页面再打开时排行榜还在、打开次数会递增，以便我知道存档真的生效了。

### 界面与反馈

73. 作为玩家，我希望左上显示基础血与分数、右上显示三个技能、底部显示伙伴栏，以便四个区各司其职。
74. 作为玩家，我希望结束时能一键重开或去看排行榜，以便闭环完整。
75. 作为验收人，我希望右下角常驻一个反馈按钮，以便我随时能提意见而不用切窗口。
76. 作为验收人，我希望点开后能一键复制状态快照，以便我把「太滑了」和数据一起发给 AI。
77. 作为验收人，我希望快照里包含全量参数值，以便「太滑」能被翻译成 `MOVE_INTERVAL=150ms` 这样的具体数字。
78. 作为验收人，我希望快照里包含最近 20 条事件日志，以便我能看到刚才到底发生了什么。
79. 作为验收人，我希望快照里有 FPS 和设备信息，以便卡顿能被归因到设备还是代码。
80. 作为验收人，我希望有个意见输入框能写主观感受，以便我不用组织成技术语言。

### AI 自主新增

81. 作为验收人，我希望 AI 能自己加新伙伴/地形/怪物/技能而不用先问我，以便我不用每次都审批。
82. 作为验收人，我希望每个新增都能在 `config.js` 里改一行就关掉，以便不喜欢就撤掉而不影响其它。
83. 作为验收人，我希望所有新增登记在一张总表里（含开关名和三步验证），以便我一眼看全加了什么。
84. 作为验收人，我希望已调好的旧数值不会被 AI 顺手改动，以便我的手感记录不会作废。
85. 作为验收人，我希望 AI 新增的东西不会破坏「伙伴被动、技能主动」这条边界，以便游戏不会长出四不像。

### 工程可维护性

86. 作为后来接手的 AI，我希望所有参数都在 `config.js`，以便我不用满代码库找数字。
87. 作为后来接手的 AI，我希望每次 commit 前跑 `check.js` 就能确认没犯硬约束，以便跨会话一致性不靠自觉。
88. 作为后来接手的 AI，我希望有 ADR 解释「为什么」，以便我不会把某个看起来多余的设计优化掉。
89. 作为验收人，我希望每个可验证阶段有一次 git commit 且历史不被改写，以便我能回溯到底改了什么。
90. 作为验收人，我希望代码里的叫法和术语表严格一致，以便几十轮之后还能检索得到。

---

## Implementation Decisions

**架构与加载**

- 15 个模块挂到全局 `window.ADV`（`config` `state` `grid` `terrain` `companions` `monsters` `skills` `collision` `models` `render3d` `input` `ui` `storage` `feedback` `main`），禁止往 `window` 直接挂别的变量
- 依赖顺序即 `index.html` 里 `<script>` 的顺序；新增模块必须同步更新该列表（否则全局报错）
- three.js r128 UMD 落 `vendor/`，全局 `THREE`；运行时零外部请求

**逻辑与渲染分离**（这是测试 seam 成立的前提）

- 主循环用固定步长累加器，逻辑步长 16.67ms；逻辑更新不引用 `THREE`、不触碰 DOM
- 渲染层只读取状态、不持有权威状态。所有权威状态在 `state.js`
- 这条不是「为了好测而设计」——`PROMPT.md` §1 本来就这么要求，测试只是顺带受益

**参数与可观测性**

- 所有可调参数集中 `config.js`；业务代码里出现写死的调参数值即违规
- 事件日志（`eventLog`，保留最近 20 条）**从第一个 commit 就要埋**，不能事后补——事后补的日志覆盖不到早期 bug（ADR-0006）
- 新增 `SEED` 参数，让地形/怪物/伙伴的生成可复现，测试才能稳定重放同一场景

**世界与内容**

- 区块 16×16，活动半径 3 生成、半径 5 回收，回收时 `dispose()` 几何体与材质
- 地形生成过四条原则校验：不聚堆 / 不形成屏障（连通性校验）/ 动态无边界 / 出生点 5×5 安全
- 受击结算严格按 `shield → companion → baseHP` 三级顺序，这是全项目最容易写错的地方
- 触屏通道实现但标注「未真机验证」，不得影响键盘手感

**自主新增权**

- AI 可直接新增伙伴 / 地形 / 怪物 / 技能，无需申请（ADR-0016）
- 对价是五条红线：不改旧数值 / 不新增按键 / 不改结算顺序 / 不改 UI 布局 / 新实体满足所属类别的本质约束
- 每个新增必带 `enabled` 开关、强度对比表、六件套，且只更新 `PROMPT.md` §9.3 总表

---

## Testing Decisions

**Seam：一个，高层，复用现有**

测试打在**无头逻辑层**——在 Node 里不加载 three.js、不启动 DOM，直接驱动逻辑循环，断言 `eventLog` 事件序列与 `state` 快照。

理由：这是**复用而非新增** seam。`eventLog` 由 ADR-0006 强制存在（它就是验收人一键复制的状态快照），`PROMPT.md` §1 已强制逻辑与渲染分离。所以零新增观测点，且能覆盖跨模块的连锁反应——「踩火山 → 队尾的龙被消耗 → 那一格变 grass」整条链一次测完，函数级单测反而看不见。

**测什么**

- 只测外部行为，不测实现细节。断言的是「发生了什么事件、状态变成了什么」，不是「调用了哪个内部函数」
- 三条最高价值回归（都是文档点名最容易错的地方）：
  1. **结算顺序**：`shield → companion → baseHP`，含护盾只吸收 1 点、溢出继续打 `baseHP`
  2. **地形摧毁链**：伙伴触发 / `slash` / BOSS 技能三种来源，摧毁后变 `grass`
  3. **互伤与 `recoil`**：普通移动撞怪双方掉血并僵直 0.5 秒；`dash`/`slash`/`guard` 不触发僵直也不自伤
- 地形生成四原则用固定种子反复生成，统计验证不聚堆、无屏障

**不测什么**

- `render3d` / `models` / `ui` / `input` 不进自动化测试——它们需要人眼判断，只做人工冒烟
- 60fps 只做人工确认（`file://` 打开看 FPS 读数），不做自动化性能断言（ADR-0001 把压测推迟到阶段二）

**配套**

- 测试文件放 `test/` 目录，**不进 `js/`**，因此不参与 `index.html` 加载、不违反 `check.js` 第 6 项
- `check.js` 9 项静态检查每次 commit 前必跑；第 9 项（总表与代码一致）刻意只警告不阻断，避免堵死自主新增权
- 每个 ticket 的验收条件必须写成可观测事件，而不是「代码写完了」

---

## Out of Scope

- 音效与背景音乐
- 账号体系、服务端、多人联机
- 构建工具（Webpack / Vite / TypeScript 一律禁止，L0-2）
- 性能压测——ADR-0001 把它推迟到阶段二，阶段一只要 60fps 流畅，不故意制造满载
- 触屏真机验证（无设备；代码保留并标注）
- 外部资源：CDN、外链图片、字体、贴图、外部模型文件
- 用 emoji 充当角色
- 第一阶段的功能裁剪（ADR-0002 明确否决）

---

## Further Notes

- **暗线目标**：ADR-0001 把本项目定位为执行环境探针——代码结构刻意做重（多模块、全量功能、长会话）。因此 tickets 分批交付是允许的（那是实现顺序），但**功能裁剪不允许**。每个 ticket 自包含，最后一个 ticket 的 context 可整体丢弃。
- **分批 ≠ 裁剪**：11 个 ticket 按顺序实现，验收人拿到的第一版仍是 21 个原生实体全量。
- **文档同步是硬责任**：改数值 → `PROMPT.md` §5；改规则 → 对应章节 + 新增 ADR；新增概念 → `GLOSSARY.md` + §9.3 总表。四份文档缺一不可。
- **命名零漂移**：`baseHP` / `armorLayer` / `recoil` / `stun` / `lavaTitan` 等标识符与术语表严格一致；`stun` 与 `recoil` 不得互换（第七节命名禁区）。
- **已接受的风险不要顺手优化**：ADR-0010 的史莱姆威胁感偏低、ADR-0018 的后期越走越安全，都是显式接受过的代价，试玩后若确实有问题，优先调新参数而不是移除机制。
