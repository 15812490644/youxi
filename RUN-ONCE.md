# RUN-ONCE.md — 给执行 AI 的任务书

> **这份文件是给 AI 读的。**把它连同本包其它文件一起交给执行方，然后说一句：
> 「装好 `skill/` 里的全部 skill，然后读 `RUN-ONCE.md`，按它把游戏做出来。」
>
> 前置条件：`skill/` 下 **25 个 skill 已安装**（`node scripts/verify-setup.js` 退出码 0）。
> 装不了 skill 的环境改用 `RUN-ONCE-NO-SKILLS.md`——同样的纪律，写成纸质指令。
> 不确定下一步用哪个 skill 时问 `/ask-matt`；它是本包所有 skills 的路由器。

---

## 0. 一句话任务

在 `index.html` 所在目录，产出一个**双击即可运行**的 3D 网页游戏《冒险家与伙伴》——变种贪吃蛇，纯前端、多文件、真 3D（three.js 本地文件）。功能一次做全，不做裁剪版。

产物目录结构（**必须严格遵守**）：

```
/
├── index.html            # 按依赖顺序加载所有脚本，双击它就能玩
├── check.js              # 本包已提供，不要修改判定规则
├── vendor/
│   └── three.min.js      # three.js r128 UMD 版，本地文件，提供全局 THREE
├── css/
│   ├── reset.css
│   ├── ui.css
│   └── sprites.css
├── js/                   # 15 个模块，一个都不能少
│   ├── config.js         # 【所有可调参数集中于此，唯一的参数来源】
│   ├── state.js
│   ├── grid.js
│   ├── terrain.js
│   ├── companions.js
│   ├── monsters.js
│   ├── skills.js
│   ├── collision.js
│   ├── models.js
│   ├── render3d.js
│   ├── input.js
│   ├── ui.js
│   ├── storage.js
│   ├── feedback.js
│   └── main.js
└── test/                 # 无头测试，不进 js/，不参与页面加载
```

---

## 1. 先装 skill（在 tracer bullet 之前）

```bash
bash scripts/install-skills.sh     # 安装 skill/ 下全部 25 个 skill（幂等，可重复跑）
node scripts/verify-setup.js       # 校验
```

**完成判据**：`verify-setup.js` 打印「全部通过」，25 个 skill 全部就位；其中 `ask-matt` / `implement` / `tdd` / `code-review` / `to-tickets` 五个必须可被当前 harness 直接调用。

装不上时（网页版 AI、无文件权限的沙箱）：改用 `RUN-ONCE-NO-SKILLS.md`，并在交付说明里写明「未走 skill 流程」。**不许跳过安装却继续按本文件执行**——§4 的循环依赖那些 skill。

---

## 2. 开工前必读（按顺序，不要跳）

| 顺序 | 文件 | 你会拿到什么 |
|---|---|---|
| 1 | **本文件** | 任务边界、红线、执行顺序 |
| 2 | `docs/GLOSSARY.md` | **术语与代码标识符**。先读它，否则后面所有命名都会错 |
| 3 | `docs/PROMPT.md` | 全部规则与**数值**（§5 是完整的 `config.js` 参数清单，直接照抄） |
| 4 | `docs/RULES.md` | 改动分级与自检清单（§4 那 11 项，做完要逐条打勾） |
| 5 | `docs/ADR.md` | 每条设计**为什么**是这样。改任何规则前先查它 |
| 6 | `docs/agents/skills-installed.md` | 25 个 skill 各自在 flow 的哪一站 |
| 7 | `spec.md` | 90 条 user stories，覆盖全部验收点 |
| 8 | `tickets/01` ~ `tickets/11` | 实现队列与每个模块的验收条件 |

**四份文档缺一不可。** 缺 `GLOSSARY.md` 会在命名上出错，缺 `RULES.md` 会在改动边界上出错，缺 `ADR.md` 会把某个有意为之的设计当成冗余给"优化"掉。

---

## 3. 交付形态的硬约束（这三条决定成败）

1. **双击 `index.html` 必须能玩。** 负责人不懂命令行，不会起本地服务器。
   → 因此**禁止 ES modules**（`file://` 下会被 CORS 拦死，双击必白屏）。只能用按依赖顺序排列的普通 `<script src>`，各模块挂到全局 `window.ADV`。
2. **运行时零外部请求。** 不得引用任何 CDN、外链图片、字体、音频。three.js 必须是本地文件。**没有网络也要能玩。**
3. **禁止任何构建工具。** 不要 Webpack / Vite / TypeScript / 打包器。项目必须保持"打开就能跑"的原始形态。

---

## 4. 执行顺序

按 `tickets/01` → `tickets/11` 的顺序做。**这个顺序就是依赖顺序**，跳着做会踩空。

| # | Ticket | 关键产出 |
|---|---|---|
| **01** | bootstrap | three.js 落 `vendor/`、加载列表、固定步长主循环、`eventLog` 埋点、`test/` 入口 |
| 02 | terrain | 4 种地形、区块生成回收、四条生成原则 |
| 03 | companions | 6 种伙伴、队伍跟随、**尾部消耗**、摧毁地形 |
| **04** | resolve-order | **受击结算三级优先级** —— 全项目最容易写错，见 §6.1 |
| 05 | monsters | 3 种普通怪、**互伤**、`recoil` 0.5 秒 |
| 06 | skills | `dash` / `guard` / `slash`，独立冷却 |
| 07 | boss | 3 种 BOSS、波次轮换、技能调度 |
| 08 | ui | 四界面 + HUD 三区 |
| 09 | storage | localStorage 排行榜 |
| 10 | feedback | 状态快照导出面板 |
| 11 | touch | 触屏通道（标注未真机验证） |

**01 是 tracer bullet，必须先打通再加内容**：如果 `file://` 下 three.js 加载失败，后面 14 个模块全是白写。

### 每个 ticket 的循环

```
/implement  ──►  内部驱动 /tdd（一次一个 red-green slice）
            ──►  /code-review（Standards + Spec 双轴）
            ──►  git commit
            ──►  /clear
            ──►  下一个 ticket
```

- 一个 ticket 一个 **fresh session**，ticket 之间 `/clear`。ticket 是自包含的，它的 context 在完成后可以整体丢弃，只留磁盘上的文件作痕迹
- ticket 的 `Status:` 开工前改成 `claimed`，完成后改成 `resolved` 并追加 `## Answer`
- `Blocked by:` 里列出的 ticket 全部 `resolved` 才可领取；同时可领多个时编号小的先领（05 与 06 都只 blocked by 04，08 同理）
- `git commit` message 写明「改了什么 + 为什么」。**禁止** `reset --hard`、禁止 `push --force`、禁止改写历史——提交链是证据链

---

## 5. 绝对不能碰的红线

**正向目标：往旁边加东西，别动已经在那儿的东西。** 下面是这条原则在具体操作上的展开。碰了就必须回滚并补一条 ADR 说明。

| 编号 | 红线 |
|---|---|
| L0-1 | 不得合并任何两个模块，不得把代码塞回单个 HTML |
| L0-2 | 不得引入构建工具 |
| L0-3 | 不得改写 git 历史 |
| L0-4 | 不得重命名 `GLOSSARY.md` 里的任何代码标识符 |
| **L0-5** | **不得静默降级**——做不了就直说并给替代方案，**严禁**用"看起来像但功能缺失"的东西顶替 |
| L0-6 | 不得删除任何已实现且未被点名的功能 |
| L0-7 | 不得重构未被点名的模块 |
| L0-8 | 不得把 `config.js` 的参数搬进业务代码，不得写死数值 |
| L0-9 | 不得引入任何外部请求 |
| L0-10 | 不得在未经说明的情况下改变任何游戏数值 |
| L0-A | 不得修改已有伙伴/地形/怪物/技能的数值 |
| L0-B | 不得新增按键（只能 `1`/`2`/`3` 或 HUD 点击） |
| L0-C | **不得改变受击结算顺序** |
| L0-D | 不得改变 UI 布局结构（左上 HP+分数、右上技能、底部伙伴栏、右下反馈） |
| L0-E | 新实体必须满足所属类别的本质约束：伙伴被动 / 技能主动且有 CD / 地形可摧毁 / 怪物有血量 / BOSS 技能可躲 |

分级与完整解释见 `docs/RULES.md` §1。

---

## 6. 三个最容易写错的地方（专门盯住）

### 6.1 受击结算顺序 —— 不可调换

```
1. shield 生效中      → 护盾先吸收 1 点，护盾消失；剩余伤害进入下一步
2. 存在对应 companion → consume 该伙伴（从队伍尾部），剩余伤害全部免除
                        （若来源是地形，额外摧毁该格地形）
3. 否则               → baseHP -= 剩余伤害
                        （若来源是危险地形，额外触发 stun 1 秒）
4. baseHP <= 0        → 游戏结束
```

护盾**不是无敌**，只是「临时一格血」。被 BOSS 撞（3 点）且无对应伙伴时：护盾吃掉 1 点，**剩下 2 点打在 `baseHP` 上**。重复获得不叠加，只重置 60 秒计时。

### 6.2 `stun` 与 `recoil` 是两个不同状态

| | `stun` 眩晕 | `recoil` 撞击硬直 |
|---|---|---|
| 触发 | 撞自己队伍（3 秒）/ 踩危险地形（1 秒） | **撞怪物**后双方僵直 |
| 时长 | 3000ms / 1000ms | 500ms |
| 影响 | 只影响冒险家 | 冒险家与怪物双方 |

### 6.3 `baseHP` 与伙伴数是两个独立数据源

- `baseHP`：独立数值，初始 3，上限 3，归 0 结束
- `armorLayer`：等于当前伙伴数量，是**实时长度**不是上限
- 界面上左上角显示 `baseHP`，底部显示伙伴队列，**绝不共用同一个数字**

§6.2 / §6.3 的权威定义在 `docs/GLOSSARY.md` 第七节（命名禁区）与第十节（状态枚举）。

---

## 7. 术语铁律

代码标识符必须与 `GLOSSARY.md` 完全一致。命名漂移视为违规。

| 禁止写 | 必须写 |
|---|---|
| `hp` / `health` / `life` / `blood` | `baseHP` |
| `snake` / `player` / `hero` | `adventurer` |
| `body` / `tail` / `segment` | `party` / `companion` |
| `item` / `powerup` / `buff` | `companion` |
| `enemy` / `mob` | `monster` |
| `map` / `level` / `screen` | `grid` / `chunk` |
| `boss` / `boss1` / `boss2` / `boss3` | `lavaTitan` / `frostKing` / `shadowLord` |
| `destroy` / `remove` / `clear`（指地形） | `terrainDestroy` |

判据速查：**玩家能不能主动放？能 → 技能，不能 → 伙伴。它会动吗？会 → 怪物，不会 → 地形。它有血量吗？有 → 可击杀，没有 → 只能被摧毁。**

---

## 8. 测试：一个 seam，无头逻辑层

不要为渲染写自动化测试。只测逻辑——`/tdd` 一次一个 red-green slice：

- 在 `test/` 下用 Node 直接驱动逻辑循环，**不加载 three.js、不启动 DOM**
- 断言 `eventLog` 的事件序列 + `state` 快照
- 主循环用固定步长累加器（逻辑步长 16.67ms），逻辑层**不得引用 `THREE`、不得触碰 DOM**——这是无头测试能成立的前提，也是 `PROMPT.md` §1 的硬要求

必须覆盖的三条黄金回归：

1. **结算顺序**：护盾只吸收 1 点，溢出继续打 `baseHP`
2. **地形摧毁链**：踩火山 → 队尾的龙被消耗 → 那一格变 `grass`（整条链，不是单点）
3. **互伤与 `recoil`**：普通移动撞怪双方掉血 + 僵直 0.5 秒；而 `dash`/`slash`/`guard` **不**自伤、**不**触发僵直

`test/` 不进 `js/`，因此不参与页面加载。

---

## 9. 完成判据（逐条对照，不许打折）

- [ ] `node scripts/verify-setup.js` 退出码 0（25 个 skill 全部就位）
- [ ] 双击 `index.html` 即可运行，控制台无报错，无外部网络请求
- [ ] 键盘四向移动、队伍跟随、禁止 180° 反向
- [ ] 6 种伙伴均可收集，能力按规格触发并从**尾部**消耗（含 `wolf` 杀怪回血）
- [ ] **伙伴触发后该格地形被摧毁变 `grass`**
- [ ] 4 种地形按权重生成，不聚堆、不形成屏障，无限延伸，区块正常回收
- [ ] 无对应伙伴踩危险地形：扣血 + 眩晕 1 秒
- [ ] 3 种普通怪行为符合定义；3 种 BOSS 按波次轮流刷新，技能各自生效
- [ ] `slash` 能砍平周围危险地形
- [ ] `panda` 击杀铁怪后获得护盾：临时一格血、1 分钟失效、不叠加只重置、被 BOSS 撞时只吃 1 点
- [ ] 撞怪物时互伤成立，双方 `recoil` 0.5 秒
- [ ] 3 个技能独立冷却，效果符合规格
- [ ] `baseHP` 与伙伴数显示为两个独立数值，结算优先级正确
- [ ] 结束 / 重开 / 排行榜闭环，Top 10 写入 `localStorage`
- [ ] 反馈面板可导出状态快照 JSON
- [ ] 稳定 60fps
- [ ] `node check.js` **9 项全通过**
- [ ] 至少 3 次 git commit，历史清晰可追溯
- [ ] `tickets/` 下 11 个 ticket 的 `Status:` 全部 `resolved`

---

## 10. 收尾三件事（缺一视为未完成）

1. **跑 `node check.js`**，9 项全绿。它校验：文件是否被合并、是否混入 ES modules、是否有外部请求、是否有命名禁区词、是否写死魔法数字、加载列表是否完整、全局命名空间是否干净、改动是否已 commit、新增实体是否已登记（第 9 项只警告不阻断）。
   误报时**加白名单并在 ADR 记一笔，不许为了过检查而改代码**。
2. **填 `RULES.md` §4 的 11 项自检清单**，把勾选结果贴给验收人。
3. **按 `RULES.md` §3 的格式报告**：分级 / 定位 / 修改 / 验证方式 / **未改动** / 自检。「未改动」一栏**不许省略**，那是验收人核对有没有被误伤的唯一依据。

---

## 11. 如果一次跑不完

**可以分批改完，但不许裁剪功能。** 「分批」是实现顺序，「裁剪」是违约（ADR-0002 明确否决先做最小可玩）。

跑到哪里算哪里时：

1. 在**模块边界**停下（不要停在半个模块中间），`git commit` 一次
2. 下一轮从 `tickets/` 里下一个未完成的编号继续，先读该 ticket 文件
3. 每轮开始时重新读 `docs/GLOSSARY.md` 与 `docs/RULES.md`——跨会话最容易丢的就是命名与红线
4. 要换 harness、换目录或交给同事时，用 `/handoff` 写一份便携交接文档（它会带上 suggested skills），别靠聊天记录

**不要为了让输出更短而省略功能。** 如果确实做不了某一项，**明确说出来并给出替代方案**，不要用占位符、`TODO`、或"简化版实现"糊弄过去——那是 L0-5 静默降级，是最严重的违约。

---

## 12. 你有权自主新增内容

你可以自己加伙伴 / 地形 / 怪物 / 技能，不需要申请。但每加一个必须配齐六件套：

`config.js` 参数（**含 `enabled` 开关**）/ 术语表条目 / 造型定义 / 克制关系 / 三步验证 / 一条 ADR

以及一张**与现有同类的强度对比表**，超出中位的地方标 `⚠️`。

加完之后**只更新 `docs/PROMPT.md` §9.3 内容总表**，不单独写报告。总表是验收人唯一的监督入口，每行必须填齐（含开关名与三步验证）。
